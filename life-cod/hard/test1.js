/*
  Вывод квадрата числа
  Необходимо написать функцию которая принимает отсортированный массив целых чисел и возвращает 
  отсортированный массив с квадратом этих чисел
*/

const sortedArray = [-4, -2, 0, 3, 5];

function sortedSquaredArray(arr) {
  return arr
}

console.log(sortedSquaredArray(sortedArray));